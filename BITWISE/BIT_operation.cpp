#include<bits/stdc++.h>
using namespace std;

#define MASK(x) ((ll)(1)<<(x))
#define BIT(mask , x) (((mask)>>(x))&(1))

int main(){
    ios::sync_with_stdio(false);
    cin.tie(0) ; cout.tie(0) ; 
    
    
}